import { featuresMetadata } from '@/lib/pageMetadata';
import FeaturesPage from './FeaturesPage';

export const metadata = featuresMetadata;
export default function Page() {
  return <FeaturesPage />;
}
